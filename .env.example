# Conexão PostgreSQL. Neon, Supabase, Railway, ou instância local servem.
# O `sslmode=require` é necessário para provedores cloud.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

# Segredos JWT. Troque por strings longas e aleatórias (ex.: `openssl rand -hex 48`).
# DEVEM ser diferentes entre si.
JWT_ACCESS_SECRET=troque-por-uma-string-aleatoria-longa
JWT_REFRESH_SECRET=outra-string-aleatoria-diferente-da-anterior
